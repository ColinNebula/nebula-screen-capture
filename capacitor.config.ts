import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nebula3d.screencapture',
  appName: 'Nebula Screen Capture',
  webDir: 'build'
};

export default config;
